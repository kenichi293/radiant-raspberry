---
import MainLayout from '../../layouts/MainLayout.astro';
---

<MainLayout title="連泊予約" currentLang="ja">
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">連泊予約</h1>

    <p class="text-center text-gray-700 mb-8">
      長期滞在をご希望の方はこちらからご予約ください。3泊以上のご宿泊には特別料金をご用意しております。
    </p>

    <form class="max-w-lg mx-auto bg-white p-6 rounded shadow-md">
      <div class="mb-4">
        <label class="block font-bold mb-2">お名前</label>
        <input type="text" name="name" class="w-full border px-3 py-2 rounded" required />
      </div>
      <div class="mb-4">
        <label class="block font-bold mb-2">メールアドレス</label>
        <input type="email" name="email" class="w-full border px-3 py-2 rounded" required />
      </div>
      <div class="mb-4">
        <label class="block font-bold mb-2">チェックイン日</label>
        <input type="date" name="checkin" class="w-full border px-3 py-2 rounded" required />
      </div>
      <div class="mb-4">
        <label class="block font-bold mb-2">宿泊日数</label>
        <input type="number" name="nights" min="3" class="w-full border px-3 py-2 rounded" required />
      </div>
      <div class="text-center">
        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">
          予約する
        </button>
      </div>
    </form>
  </main>
</MainLayout>
